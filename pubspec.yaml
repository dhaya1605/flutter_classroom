name: eclass
description: A new Flutter application.

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^0.1.2
  firebase_core: ^0.4.4+3
  firebase_auth: ^0.16.0
  cloud_firestore: ^0.13.5
  modal_progress_hud: ^0.1.3
  google_sign_in: ^4.4.2
  fluttertoast: ^4.0.1
  shared_preferences: ^0.5.6+3
  short_readable_id: ^1.5.1
  flutter_speed_dial: ^1.2.5
  full_screen_menu: ^0.1.2
  popup_menu: ^1.0.5
  firebase_storage: ^3.1.5
  image_picker: ^0.6.5+2
  video_player: ^0.10.9
  chewie: ^0.9.10
  file_picker: ^1.3.6
  flutter_playout: ^1.0.35
  auto_orientation: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter


# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - images/login1.png
    - images/log-pic.jpg
    - images/home.png
    - images/secondary_icon.png
    - images/login.jpg
    - images/quiz.png
    - images/assignment.png

  fonts:
     - family: marienda
       fonts:
         - asset: fonts/Merienda-Bold.ttf
         - asset: fonts/Merienda-Regular.ttf
#           style: italic
     - family: sketch
       fonts:
         - asset: fonts/Cabin_Sketch/CabinSketch-Bold.ttf
         - asset: fonts/Cabin_Sketch/CabinSketch-Regular.ttf
     - family: coda
       fonts:
         - asset: fonts/Coda/Coda-ExtraBold.ttf
         - asset: fonts/Coda/Coda-ExtraBold.ttf
     - family: lemonada
       fonts:
         - asset: fonts/Lemonada/static/Lemonada-Regular.ttf
         - asset: fonts/Lemonada/static/Lemonada-Bold.ttf

  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages
